name: flutter_api_test
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  amazon_cognito_identity_dart_2: ^3.3.0
  shared_preferences: ^2.1.1
  flutter_dotenv:
  http:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
